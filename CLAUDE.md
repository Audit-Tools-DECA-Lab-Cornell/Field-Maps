# CLAUDE — field-ops quick reference

FieldOps Parcel Editor. Next.js App Router prototype, pnpm. See `AGENTS.md` for
the routing card and `README.md` for `create-next-app` boilerplate (not yet
replaced with project-specific setup docs).

## Commands

```bash
pnpm dev
pnpm build
pnpm lint
pnpm format
```
