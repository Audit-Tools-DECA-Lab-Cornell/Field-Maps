# Contracts shared by web, mobile and API

This file is part of the [FieldMaps production plan](README.md) and defines the `CON-*` tasks. It is the single reference for:
- the shapes that cross a component boundary;
- the API endpoint catalog;
- the canonical form definition;
- the observation envelope;
- the error envelope.

Backend tasks in [backend/PLAN.md](../../backend/PLAN.md) implement these shapes. Web tasks in [web/PLAN.md](../../web/PLAN.md) and mobile tasks in [mobile/PLAN.md](../../mobile/PLAN.md) consume them. If a shape must change, change it here first, then update every task that consumes it. The `cross-repo-contract-change` skill describes the procedure.

## Context: why contracts are needed now

Today the mobile form format and the server form format disagree:

| | Mobile (`mobile/src/forms/definition.ts`) | Server (`backend/src/fieldmaps_api/forms.py`) |
|---|---|---|
| Question key | `id` | `code` |
| Kind names | `kind: one \| many \| text \| number` | `type: choice \| multi-choice \| integer \| …` |
| Range names | `min` / `max` | `minimum` / `maximum` |
| Conditions | `dependsOn` conditions | none |
| Hidden required fields | optional | rejected when missing (`forms.py:50-55`) |

API types are also written by hand twice: `web/src/lib/packages.ts:17-37` and `mobile/src/sync/contracts.ts`. And the `janet-test-v1` form cannot upload.

## The `contracts/` folder (repository root)

It holds language-neutral JSON only, with no shared JavaScript or Python package. Each app keeps its own dependencies, as `docs/Workspace.md` requires.

```
contracts/
  README.md                       rules below, who generates and who consumes each file
  openapi.json                    generated by BE-04; never edited by hand
  form-definition.schema.json     generated from mobile zod (CON-02); never edited by hand
  forms/<code>.json               canonical form definitions (hand-maintained, reviewed)
  forms/cases/<code>.cases.json   answers → expected visible question ids + errors
```

## Canonical form definition

**The mobile format is canonical.** It already carries versioned questions, `act` groups, restricted declarative conditions (`answered | equals | notEquals | includes | all | any`), dynamic option sets, `exportColumn`, provenance, and validation for duplicates, dangling references and cycles.

Rules both sides must implement identically:
1. **Answers are keyed by question `id`.** `exportColumn` is used only for GIS and exports.
2. **Visibility.** A question is visible when its condition holds over the current answers. Answers to hidden questions are dropped before saving.
   - The device drops them.
   - The server drops any that still arrive, and reports their ids as `pruned` in the per-operation result. It never rejects the observation for them: a parity bug must cost one hidden answer, not a record.
3. **Required.** A question is required only while it is visible.
4. **Types.** `number` answers are JSON numbers (the mobile code currently stores strings, `mobile/src/domain/observation.ts:38`). `one` answers are an option code. `many` answers are an array of distinct option codes. `text` answers respect `maxLength`.
5. **Workbook rule text** is provenance only and is never executed.
6. **`status`** now lives on the server row (`form_versions.state`, DB-09), not inside the definition. The definition's own `status` field is ignored once DB-09 lands.
7. **The JSON Schema describes the input.**
   - It is generated with `io: "input"`, so defaulted fields are optional and unknown keys are allowed.
   - The server's models use `extra="ignore"` and the same defaults, so the server accepts every definition the device accepts.
8. **Retired versions stay valid.** Records collected on a version that is later retired still upload and still render (DB-09, BE-12).

## Observation envelope (sync upload and storage)

```json
{
  "id": "uuid (client-generated)",
  "project_id": "uuid",
  "site_id": "uuid",
  "form_version_id": "uuid",
  "zone_code": "A | null",
  "package_version": 3,
  "round": 2,
  "context": { "fresh_period": true, "inherited_from": "uuid | null" },
  "gps_accuracy_m": null,
  "longitude": -76.485, "latitude": 42.448,
  "observed_at": "2026-10-02T14:03:11.412-04:00",
  "observer_code": "JS",
  "answers": { "<question id>": "value" },
  "device_id": "uuid", "app_version": "1.0.0 (42)"
}
```

The current `PUT /v1/projects/{p}/observations/{uuid}` body (`site_id`, `form_version` code, `coordinates`, `observer`, flat answers) is accepted until MOB-10 ships, and is removed after the pilot.

## Error envelope

```json
{ "error": { "code": "membership_required", "message": "Human sentence.", "details": {} } }
```

**Codes.** Extend this table before using a new code. Codes raised inside the database travel as the SQLSTATE shown, and BE-02 translates them.

| Code | HTTP | SQLSTATE |
|---|---|---|
| `unauthenticated` | 401 | |
| `token_invalid` | 401 | |
| `account_deleted` | 403 | `FM005` |
| `membership_required` | 403 | |
| `role_required` | 403 | `FM006`, `42501` |
| `limit_reached` | 403 | `FM001` |
| `not_found` | 404 | `FM007` |
| `invitation_invalid` | 404 | `FM003` |
| `conflict` | 409 | `FM008`, `23505` |
| `sole_owner` | 409 | `FM002` |
| `invitation_expired` | 410 | `FM004` |
| `archive_unavailable` | 410 | |
| `validation_failed` | 422, or 413 for an oversize body | `23514`, `23502`, `23503`, class `22` |
| `unsupported_operation` | per-operation only | |
| `rate_limited` | 429 | |
| `internal` | 500 | |
| `storage_unavailable` | 503 | transient classes (BE-02) |

`validation_failed` carries `details.fields: [{ "id": "<question id or field>", "problem": "…" }]`.

## API endpoint catalog (v1)

This is the full target surface. **Exists** means it is already in `backend/src/fieldmaps_api/main.py`. The owning task implements each endpoint.

| Module | Method and path | Who | Owner |
|---|---|---|---|
| — | `GET /health` · `GET /ready` | anyone | exists · BE-02 |
| identity | `GET /v1/me`: profile, org and project memberships; creates the profile and Training membership on first call (403 `account_deleted` for a forgotten account) | any verified user | BE-06 |
| identity | `PATCH /v1/me` (display name, initials, locale) | self | BE-06 |
| identity | `DELETE /v1/me`: 204 when done, 202 `{"status":"pending"}` when the Auth deletion must be retried; safe to repeat | self | BE-08 |
| tenancy | `POST /v1/orgs` (org + owner membership + first project) · `GET /v1/orgs` | verified user (limit 3 orgs) | BE-07 |
| tenancy | `GET/PATCH /v1/orgs/{org}` · `GET /v1/orgs/{org}/members` · `PATCH/DELETE /v1/orgs/{org}/members/{user}` · `POST /v1/orgs/{org}/transfer-ownership`. There is no POST for members: people join through invitations | org owner/admin (owners only for `owner`/`admin` rows) | BE-07 |
| tenancy | `POST/GET /v1/orgs/{org}/projects` · `GET/PATCH /v1/projects/{p}` | org admin / project member | BE-07 |
| tenancy | `GET /v1/projects` | member | exists (becomes part of BE-07) |
| tenancy | `GET /v1/projects/{p}/members` · `PATCH/DELETE /v1/projects/{p}/members/{user}` (no POST) | manager | BE-07 |
| tenancy | `POST/GET /v1/projects/{p}/invitations` · `DELETE /v1/projects/{p}/invitations/{id}` | manager | BE-07 |
| tenancy | `POST/GET /v1/orgs/{org}/invitations` · `DELETE /v1/orgs/{org}/invitations/{id}` (roles `member`, `admin`; `admin` needs an owner) | org owner/admin | BE-07 |
| tenancy | `POST /v1/invitations/preview` (`{token}` or `{code}`, consumes nothing) · `POST /v1/invitations/redeem` | verified user | BE-07 |
| sites | `GET/POST /v1/projects/{p}/sites` · `GET /v1/projects/{p}/sites/{s}/zones` | member / manager | BE-13 |
| sites | `POST /v1/projects/{p}/packages` · `GET …/packages` · `GET …/packages/{id}` | manager / member | exists (moved to Storage by BE-13) |
| sites | `GET …/packages/{id}/archive` | member | exists (kept for web download) |
| sites | `GET …/packages/{id}/download`: short-lived signed Storage URL plus sha256 | member | BE-13 |
| instrument | `GET /v1/projects/{p}/forms` · `GET …/forms/{f}/versions` (published and retired; drafts for managers) | member | BE-11 |
| instrument | `POST /v1/projects/{p}/forms` | manager | BE-11 |
| instrument | `POST …/forms/{f}/versions` (draft from JSON) · `POST …/versions/{v}/publish` · `POST …/versions/{v}/retire` | manager | BE-11 |
| collection | `POST /v1/sync/upload`: a PowerSync CRUD batch, with one result per operation (see below) | observer / manager | BE-12 |
| collection | `PUT /v1/projects/{p}/observations/{uuid}` · `GET …/observations/{uuid}` | observer / member | exists (legacy after MOB-10) |
| analysis | `GET /v1/projects/{p}/observations` (cursor on `(observed_at,id)`; filters: site, zone, round, form version, observer, date range, bbox) | viewer and up | BE-14 |
| analysis | `GET /v1/projects/{p}/summary` | viewer and up | BE-14 |
| analysis | `GET /v1/projects/{p}/exports?format=csv\|geojson&…filters` (streamed, audited) | viewer and up | BE-14 |
| gis | `GET /v1/projects/{p}/gis-access` (pilot: list grants and connection info, no secrets) | manager | BE-15 |

### `POST /v1/sync/upload` semantics

These follow PowerSync's guidance: <https://docs.powersync.com/handling-writes/writing-client-changes.md>.

**Request.** `{ "operations": [ { "op": "PUT|PATCH|DELETE", "table": "observations", "id": "uuid", "data": { …envelope } } ] }`.
- The server validates **each operation on its own**. A malformed operation becomes a rejected result, never a request-level 422.
- The body limit is 4 MB (BE-09). The client sizes batches to fit, and halves the batch on 413.

**Response.** 200 with `{ "results": [ { "id", "status": "accepted|rejected", "code"?, "pruned"?: ["<question id>"], "user_id" } ] }`, exactly one result per operation.
- Every accepted operation is committed **synchronously** before the response.
- A replay is accepted when it matches the stored row's canonical content (excluding `device_id` and `app_version`), or when the stored row came from the legacy `PUT`. It is never matched by raw body hash (BE-12 step 3).
- Validation, permission, conflict and unexpected per-operation failures are `rejected`. The payload is written to `fieldmaps.upload_rejections` (DB-10) through a savepoint, and syncs back to the user. A later accepted upload of the same id marks that rejection resolved.

**Other statuses.**
- 401 when the token is invalid; the client refreshes and retries.
- 503 for transient faults, such as a lost connection, deadlock or timeout. Operations answered before it stay committed. The client retries the batch, and the replays are accepted.

**The client's rule (MOB-10).** Call `.complete()` only on a 200 that parses, has one result per operation id, and carries the database owner's `user_id`. Throw on everything else, including 4xx and non-JSON 200s. PowerSync reverts any write that is completed but not applied.

**Scope.** v1 accepts only `PUT` on `observations`. Any other table or operation is rejected with code `unsupported_operation`.

## Tasks

### CON-01: Create the `contracts/` folder and its rules
Status: todo · Phase 0 · Size S · Depends: none · Blocks: CON-02, CON-03
Read first: this file; `docs/Workspace.md` (dependency boundaries).
Do:
1. Create `contracts/README.md` with the tree above, what generates each file, and who consumes it.
2. Add the `contracts/` row to the routing table in the root `AGENTS.md` and to `README.md`'s layout block.

Done when:
- the folder exists with its README;
- `AGENTS.md` routes to it.

Verify: `node docs/plan/check-plan.mjs`.

### CON-02: Make the form definition canonical and share test cases
Status: todo · Phase 0 · Size M · Depends: CON-01 · Blocks: BE-10, DB-07, DB-09, GIS-01, MOB-13
Read first:
- `mobile/src/forms/definition.ts`, `mobile/src/forms/engine.ts`, `mobile/src/forms/fixtures/*.ts`
- `backend/src/fieldmaps_api/forms.py`
- `docs/Janet-Test-Form-Scope.md`

Do:
1. Add a mobile script, `pnpm --dir mobile contracts:forms`. It writes `contracts/form-definition.schema.json` with `z.toJSONSchema(formDefinitionSchema, { io: "input" })`.
   - The default output mode would mark defaulted fields required and forbid unknown keys. The server would then reject definitions the device accepts.
2. Export `shell-v1` and `janet-test-v1` to `contracts/forms/<code>.json`. Mobile tests load these JSON files, and the TS fixtures become thin imports of the JSON, or are removed.
3. Write `contracts/forms/cases/janet-test-v1.cases.json`, with at least 20 cases. Cover:
   - wildlife branch on, then off;
   - both play slots independent;
   - a required question hidden, then shown;
   - a `many` answer with a duplicate;
   - a number out of range;
   - an unknown option;
   - text over `maxLength`;
   - a definition that omits every defaulted field and has one unknown key (both sides accept it);
   - a hidden question's answer present in the payload (it is pruned, not rejected).
4. Make the mobile engine's tests run every case file.
5. Change number answers to JSON numbers in the mobile domain (`mobile/src/domain/observation.ts:38`) and the builder, with a test.

Done when:
- the schema and JSON definitions are generated or committed;
- the mobile tests run the shared cases and pass.

Server parity is BE-10's job, against the same case files. This task is finished in Phase 0.

Verify: `pnpm --dir mobile test`, then `git status contracts/` shows no drift after re-running the script.

### CON-03: Generate OpenAPI types for web and mobile
Status: todo · Phase 0 · Size S · Depends: BE-04, CON-01 · Blocks: CON-04, MOB-04, WEB-05
Read first: `web/src/lib/packages.ts`, `mobile/src/sync/contracts.ts`, `backend/Makefile` (or the root `Makefile`).
Do:
1. Add `openapi-typescript` as a dev dependency in `web/` and in `mobile/` (each app's own lockfile).
2. Add the scripts:
   - `pnpm --dir web api:types`, which writes `web/src/lib/api/schema.d.ts`;
   - `pnpm --dir mobile api:types`, which writes `mobile/src/data/api/schema.d.ts`.
   Both read `contracts/openapi.json`.
3. Add a root script, `pnpm contracts:generate`, that runs BE-04's dump and then both generators.
4. Commit the generated files. When CI exists (OPS-06), add a `contracts` job that runs the generator and fails on diff; OPS-06 adds it if it lands later.

Done when:
- both apps compile against the generated types;
- regenerating produces no diff.

Verify: `pnpm contracts:generate && git diff --exit-code -- contracts web/src/lib/api mobile/src/data/api`.

### CON-04: Document the error envelope and implement one client helper per app
Status: todo · Phase 1 · Size S · Depends: BE-03, CON-03 · Blocks: MOB-04, WEB-05
Read first: the Error envelope section above; `mobile/src/sync/upload.ts` (status-code branching today).
Do:
1. Add the envelope as a component schema in the OpenAPI output (BE-03 implements it).
2. Web: `web/src/lib/api/errors.ts` maps codes to typed errors and user copy.
3. Mobile: `mobile/src/data/api/errors.ts` does the same, with copy suited to field use.

Done when:
- neither app branches on a raw status code except 401 and 5xx;
- every code in the list has copy.

Verify: unit tests in both apps for three codes each.
